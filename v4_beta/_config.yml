encoding: UTF-8
timezone: America/Chicago

defaults:
  - scope:
      path: "assets/img"
    values:
      image: true

collections_dir: collections
collections:
  - news
  - posts

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
